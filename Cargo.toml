[package]
name = "timer_test"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
tokio-websockets = { version = "0.4", features = ["client", "server", "sha1_smol", "fastrand"] }
futures-util = { version = "0.3", features = ["sink"] }
http = "0.2"
